import React from "react";

const Spinner: React.FC = () => {
  return (
    <div className="w-6 h-6 border-4 border-t-transparent border-black animate-spin rounded-full"></div>
  );
};

export default Spinner;
